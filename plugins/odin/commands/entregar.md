---
description: Executar uma entrega — spec, branch, implementação TDD e PR
argument-hint: <entrega do plano, pedido de código ou issue>
---

# /entregar

Use a skill `entregar`.

Pedido inicial: $ARGUMENTS

Rode o portão de desafio (Step 0.5) antes de planejar. Siga entrega → SPEC aprovada → branch →
implementação (Solo ou dev-loop, com portão TDD) → validações → commit/PR, sempre com OK explícito
por step. Registre o rastro no Log de execução do arquivo da entrega.
