---
description: Consultar ou atualizar os defaults locais do Odin
argument-hint: [show|set campo=valor]
---

# /defaults

Carregue `skills/entregar/references/defaults.md`.

Pedido inicial: $ARGUMENTS

Mostre ou atualize `~/.claude/odin/defaults.md` sem iniciar nenhum fluxo de entrega.

Nunca salve tokens, credenciais ou PII. Se alterar algum campo, confirme o valor com o usuário
antes de gravar.
