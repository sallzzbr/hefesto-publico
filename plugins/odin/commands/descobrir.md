---
description: Abrir o Diamante 1 — investigar o problema com evidência
argument-hint: <desafio (missão) ou problema a investigar>
---

# /descobrir

Use a skill `descobrir`.

Pedido inicial: $ARGUMENTS

Comece pelo diagnóstico de entrada e pela capa do desafio (crie se não existir, com OK no slug).
Levante a evidência você (IA) — histórico próprio, dados, código, fora do produto — e entregue o
mapa do problema em `descobertas.md`. Não conclua qual é o problema: isso é o GATE 1 + `definir`.
